# Production API Server Base URL
# Leave empty in development to use Vite's built-in proxy (localhost:5000)
# Set to your deployed server URL in production (e.g. https://your-backend-api.onrender.com)
VITE_API_URL=
